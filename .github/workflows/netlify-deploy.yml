name: Netlify Deploy (disabled)

# Disabled; Netlify deploys directly from GitHub. Kept as a placeholder to avoid re-adding.
on:
  workflow_call:

jobs:
  noop:
    if: false
    runs-on: ubuntu-latest
    steps:
      - run: echo "disabled"

